# Админ панель - Nika Project

Админ панель для управления контентом сайта с аутентификацией, построенная с использованием shadcn/ui компонентов в том же стиле, что и основной сайт.

## 🚀 Доступ

Админ панель доступна по адресу: `/admin`

Ссылка на админ панель добавлена в футер сайта (маленьким шрифтом для администраторов).

## 🔒 Безопасность и сессии

### Очистка сессии

При каждом переходе из админ панели на основной сайт автоматически очищается сессия:

- **localStorage**: Удаляются все ключи, связанные с админкой
- **sessionStorage**: Очищается сессионное хранилище админки
- **Переменные состояния**: Удаляются настройки темы, предпочтения и другие данные

### Когда очищается сессия:

1. При клике на кнопку "На сайт" в навигации админки
2. При любом переходе по ссылкам, ведущим за пределы `/admin/*`
3. При изменении URL браузера (popstate события)

Это обеспечивает безопасность и предотвращает утечку данных админ панели на основной сайт.

## 🔐 Аутентификация

Для входа в админ панель требуется авторизация. Используйте следующие учетные данные:

### Демо аккаунты:
- **Логин:** `admin` / **Пароль:** `admin123`
- **Логин:** `nika` / **Пароль:** `nika2025`

### Функции аутентификации:
- ✅ Автоматический вход при перезагрузке страницы (сессия сохраняется 24 часа)
- ✅ Защищенные роуты - неавторизованные пользователи перенаправляются на страницу входа
- ✅ Кнопка "Выйти" в верхней панели
- ✅ Валидация логина и пароля
- ✅ Показ/скрытие пароля

### 🔧 Настройка в продакшене

Для изменения учетных данных отредактируйте массив `validCredentials` в файле `src/components/admin-auth.tsx`:

```typescript
const validCredentials = [
  { username: 'your-admin', password: 'your-secure-password' },
  { username: 'another-user', password: 'another-password' }
];
```

Для интеграции с реальным API замените функцию `login` в `AuthProvider`.

## 📋 Функциональность

### Разделы для управления:

1. **Главная страница (Hero)**
   - Редактирование заголовков, описаний и кнопок
   - Управление призывом к действию
   - Настройка ссылок

2. **Обо мне (About)**
   - Редактирование информации о себе
   - Управление ключевыми достижениями
   - Добавление/удаление пунктов

3. **Процесс работы (Process)**
   - Редактирование шагов процесса
   - Управление описаниями и примерами
   - Настройка деталей каждого шага

4. **Отзывы (Testimonials)**
   - Просмотр всех отзывов клиентов
   - Добавление новых отзывов
   - Редактирование существующих отзывов
   - Удаление отзывов

5. **Услуги (Services)**
   - Управление услугами и ценами
   - Настройка доступности услуг
   - Редактирование описаний и характеристик

6. **Клиенты (Clients)**
   - Управление сегментами клиентов
   - Редактирование описаний целевых аудиторий

7. **Проекты (Projects)**
   - Управление кейсами и завершенными проектами *(в разработке)*

8. **Заявки (Inquiries)**
   - Просмотр входящих заявок от клиентов *(в разработке)*

## 🎨 Дизайн

Админ панель использует:
- Тот же цветовой стиль (светлая/темная тема)
- Шрифты Montserrat и Open Sans
- shadcn/ui компоненты (Card, Button, Input, Table, Dialog, etc.)
- Закругленные углы и современные компоненты
- Адаптивный дизайн

## 🔧 Технические детали

- **UI библиотека**: shadcn/ui
- **Роутинг**: wouter
- **Состояние**: React hooks (useState)
- **Формы**: Контролируемые компоненты
- **Моки**: Данные на основе `src/data/content.ts`

## 🚀 В продакшене

Для продакшена замените моки на реальный API:

```typescript
// В каждом компоненте замените useState с моками на API вызовы
// Например, в TestimonialsAdmin:

const [testimonials, setTestimonials] = useState([]);

// Заменить на:
useEffect(() => {
  fetch('/api/testimonials')
    .then(res => res.json())
    .then(data => setTestimonials(data));
}, []);
```

## 📝 Кастомизация

Админ панель легко расширяется:
- Добавляйте новые разделы в `src/pages/admin.tsx`
- Используйте готовые shadcn/ui компоненты
- Следуйте паттернам существующих компонентов
- Добавляйте валидацию форм при необходимости

## 🗂️ Структура файлов

```
src/pages/admin.tsx          # Основной файл админ панели
src/data/content.ts          # Исходные данные для моков
src/components/ui/           # shadcn/ui компоненты
```
