# Nika Project - Landing Page

Landing page для консалтинговых услуг Nika Shikhlinskaya с админ панелью и SQLite базой данных, разработанный с использованием React, TypeScript, Tailwind CSS и shadcn/ui.

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Инициализация базы данных (один раз)
npm run migrate

# Запуск backend сервера
npm run server

# В другом терминале - запуск frontend в режиме разработки
npm run dev

# Сборка для продакшна
npm run build

# Предпросмотр сборки
npm run preview
```

### Backend API

Проект включает REST API на Express.js с SQLite базой данных:

- **Порт**: 3001 (development), автоматически в production
- **База данных**: SQLite с файлом `data/app.db`
- **Документация API**: Все endpoints доступны по префиксу `/api/`

Доступные endpoints:
- `GET/POST/PUT/DELETE /api/hero` - Главная страница
- `GET/PUT /api/about` - Раздел "Обо мне"
- `GET/POST/PUT/DELETE /api/process-steps` - Шаги процесса
- `GET/POST/PUT/DELETE /api/client-segments` - Сегменты клиентов
- `GET/POST/PUT/DELETE /api/services` - Услуги
- `GET/POST/PUT/DELETE /api/testimonials` - Отзывы
- `GET/POST/PUT/DELETE /api/projects` - Проекты
- `GET/POST/PUT/DELETE /api/inquiries` - Заявки

### Админ панель

Админ панель доступна по адресу `/admin` и позволяет управлять контентом сайта:

- **Главная страница** - редактирование заголовков и описаний
- **Обо мне** - управление информацией и достижениями
- **Процесс работы** - редактирование шагов процесса
- **Отзывы** - управление отзывами клиентов
- **Услуги** - редактирование услуг и цен
- **Клиенты** - управление сегментами клиентов
- **Проекты** - управление кейсами *(в разработке)*
- **Заявки** - просмотр входящих заявок *(в разработке)*

Админ панель использует тот же дизайн и тему, что и основной сайт.

## 📦 Деплой на Linux сервер

### Предварительные требования

Убедитесь, что на сервере установлены:
- Docker (>= 20.10)
- Docker Compose (>= 2.0)
- Git

```bash
# Установка Docker на Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

### Процесс деплоя

1. **Клонирование проекта на сервер**

```bash
git clone <your-repo-url> nika-project
cd nika-project
```

2. **Запуск автоматического деплоя**

```bash
# Для продакшн окружения (по умолчанию)
./deploy.sh

# Для staging окружения
./deploy.sh staging

# Просмотр справки
./deploy.sh --help
```

### Ручной деплой (альтернативный способ)

```bash
# Сборка и запуск через Docker Compose
docker-compose up --build -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

## 🏗️ Архитектура деплоя

### Docker контейнеры

- **nika-project**: Nginx сервер с собранным React приложением
- Порт: 80 (HTTP)

### Структура файлов

```
nika-project/
├── Dockerfile              # Мульти-стадийная сборка
├── docker-compose.yml      # Оркестрация контейнеров
├── nginx.conf             # Конфигурация Nginx
├── deploy.sh              # Скрипт автоматизации деплоя
├── .dockerignore          # Исключения для Docker
└── dist/                  # Собранное приложение (создается автоматически)
```

## 🔧 Конфигурация

### Переменные окружения

При необходимости добавьте переменные в `docker-compose.yml`:

```yaml
environment:
  - NODE_ENV=production
  - API_URL=https://api.example.com
```

### Nginx настройки

Основные настройки в `nginx.conf`:
- Gzip компрессия
- Кеширование статических ресурсов
- Security headers
- SPA routing для React Router

## 📊 Мониторинг

### Проверка здоровья

```bash
# Health check endpoint
curl http://localhost/health

# Статус контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f nika-project
```

### Метрики производительности

- Nginx access/error логи: `/var/log/nginx/`
- Docker логи: `docker-compose logs`
- Системные ресурсы: `docker stats`

## 🔄 Обновление

### Автоматическое обновление

```bash
# Скачивание обновлений
git pull origin main

# Перезапуск с новыми изменениями
./deploy.sh
```

### Ручное обновление

```bash
# Пересборка контейнера
docker-compose build --no-cache

# Перезапуск
docker-compose up -d
```

## 🛡️ Безопасность

### Рекомендации

1. **SSL/TLS**: Настройте HTTPS с помощью Let's Encrypt
2. **Firewall**: Ограничьте доступ к порту 80/443
3. **Обновления**: Регулярно обновляйте Docker образы
4. **Мониторинг**: Настройте логирование и алерты

### SSL настройка (пример с Certbot)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx

# Получение сертификата
sudo certbot --nginx -d yourdomain.com

# Автоматическое продление
sudo crontab -e
# Добавить: 0 12 * * * /usr/bin/certbot renew --quiet
```

## 🐛 Устранение неполадок

### Распространенные проблемы

1. **Порт 80 занят**
```bash
# Проверить, что использует порт
sudo lsof -i :80
# Остановить конфликтующий сервис
sudo systemctl stop apache2
```

2. **Контейнер не запускается**
```bash
# Проверить логи
docker-compose logs nika-project

# Проверить ресурсы
docker system df
```

3. **Приложение не загружается**
```bash
# Проверить Nginx конфигурацию
docker exec nika-project nginx -t

# Проверить статические файлы
docker exec nika-project ls -la /usr/share/nginx/html/
```

4. **Проблемы с базой данных**
```bash
# Проверить файл базы данных
ls -la data/app.db

# Переинициализировать базу данных
rm data/app.db && npm run migrate

# Проверить логи сервера
npm run server:dev
```

### Логи и отладка

```bash
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f nika-project

# Логи Nginx внутри контейнера
docker exec nika-project tail -f /var/log/nginx/error.log
```

## 📁 Структура проекта

```
nika-project/
├── data/                    # SQLite база данных
│   └── app.db              # Файл базы данных (создается автоматически)
├── dist/                   # Собранное приложение
├── public/                 # Статические файлы
├── server/                 # Backend сервер
│   ├── index.js           # Основной серверный файл
│   └── src/
│       └── database.js    # Работа с базой данных
├── scripts/               # Скрипты миграции
│   └── migrate.cjs        # Инициализация базы данных
├── src/                   # Frontend исходники
│   ├── components/        # React компоненты
│   ├── data/             # Статические данные
│   ├── hooks/            # React хуки
│   ├── lib/              # Утилиты и API клиент
│   └── pages/            # Страницы приложения
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose
├── nginx.conf           # Nginx конфигурация
├── package.json         # Зависимости и скрипты
└── tailwind.config.ts   # Tailwind CSS конфигурация
```

## 📝 Скрипты

### Основные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск frontend в режиме разработки |
| `npm run build` | Сборка приложения для продакшна |
| `npm run server` | Запуск backend сервера |
| `npm run server:dev` | Запуск сервера с авто-перезагрузкой |
| `npm run migrate` | Инициализация/миграция базы данных |
| `npm run check` | Проверка TypeScript типов |

### Деплой команды

| Команда | Описание |
|---------|----------|
| `./deploy.sh` | Полный цикл деплоя |
| `./deploy.sh staging` | Деплой в staging окружение |
| `docker-compose up -d` | Запуск контейнеров |
| `docker-compose down` | Остановка контейнеров |
| `docker-compose restart` | Перезапуск сервисов |
| `docker-compose logs -f` | Мониторинг логов |

## 💾 База данных

### SQLite структура

Проект использует SQLite базу данных для хранения всех данных:

#### Таблицы:
- **hero** - Главная страница
- **about** - Раздел "Обо мне"
- **process_steps** - Шаги процесса работы
- **client_segments** - Сегменты клиентов (с иконками)
- **services** - Услуги и цены
- **testimonials** - Отзывы клиентов
- **projects** - Проекты и кейсы
- **inquiries** - Входящие заявки
- **admin_sessions** - Сессии администратора

#### Миграции:
```bash
# Инициализация базы данных с начальными данными
npm run migrate

# Резервное копирование (вручную)
cp data/app.db data/backup-$(date +%Y%m%d).db
```

#### Работа с данными:
- Данные автоматически инициализируются из `src/data/content.ts`
- Все изменения сохраняются в реальном времени
- Поддержка транзакций для целостности данных
- WAL режим для лучшей производительности

## 🔍 Производительность

### Оптимизации

- **Gzip компрессия**: Включена для текстовых ресурсов
- **Кеширование**: Статические ресурсы кешируются на 1 год
- **Мульти-стадийная сборка**: Минимальный размер образа
- **SPA routing**: Корректная обработка React Router
- **SQLite WAL**: Write-Ahead Logging для быстрой работы с БД

### Метрики

```bash
# Размер образа
docker images nika-project

# Использование ресурсов
docker stats

# Nginx статистика
docker exec nika-project nginx -V

# Размер базы данных
ls -lh data/app.db
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь в корректности конфигурации
3. Проверьте системные ресурсы
4. Проверьте сетевые настройки

Проект готов к деплою на Linux сервер с Ubuntu 20.04+ или аналогичными дистрибутивами.
